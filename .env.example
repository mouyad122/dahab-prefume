# Database URL
# Local development: SQLite (default in prisma/schema.prisma)
# Production: Use managed PostgreSQL (Neon, Supabase, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/dahab_perfumes"

# Application
NODE_ENV=development

# Admin Authentication (TODO: implement when admin routes are built)
# ADMIN_SECRET_KEY=change-me-in-production
